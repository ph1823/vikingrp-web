{% extends 'base.html.twig' %}

{% block title %}Skin{% endblock %}

{% block body %}
    <h1>Skin</h1>

    {{ form_errors(skinForm) }}

    {{ form_start(skinForm) }}
    {{ form_row(skinForm.skinImage) }}

    <button type="submit" class="btn">Register</button>
    {{ form_end(skinForm) }}
{% endblock %}
